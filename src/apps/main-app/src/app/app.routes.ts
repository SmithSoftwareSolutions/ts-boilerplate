import { Route } from '@angular/router';
import { DesignSystemPreviewViewComponent } from '@org/angular/core';

export const appRoutes: Route[] = [
  {
    path: '',
    component: DesignSystemPreviewViewComponent,
  },
];
